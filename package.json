{
  "name": "vtex.invoice-notifier",
  "scripts": {
    "lint": "cd node && tsc --noEmit --pretty && eslint --ext ts .",
    "deploy": "releasy -f manifest.json --stable"
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "eslint-config-vtex": "^11.0.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.3"
  }
}
